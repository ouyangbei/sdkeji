module.exports = {};

function define( key, value ) {
  module.exports[key] = value;
}

define( '-1', {
   'code'    : '-1',
   'message' : '失败'
} );

define( '0', {
   'code'    : '0',
   'message' : '成功'
} );


define( '1000', {
   'code'    : '1000',
   'message' : '用户名不能为空'
} );

define( '1001', {
   'code'    : '1001',
   'message' : '手机号不能为空'
} );

define( '1002', {
   'code'    : '1002',
   'message' : '验证码发送失败'
} );

define( '1003', {
   'code'    : '1003',
   'message' : '密码不能为空'
} );

define( '1004', {
   'code'    : '1004',
   'message' : '手机验证码不能为空'
} );

define( '1005', {
   'code'    : '1005',
   'message' : '手机验证码错误'
} );

define( '1006', {
   'code'    : '1006',
   'message' : '用户名已存在'
} );

define( '1007', {
   'code'    : '1007',
   'message' : '用户名密码错误'
} );

define( '1008', {
   'code'    : '1008',
   'message' : '用户不存在'
} );

define( '1009', {
   'code'    : '1009',
   'message' : '用户信息Id不能为空'
} );

define( '1010', {
   'code'    : '1010',
   'message' : '用户信息更新失败'
} );
define( '1025', {
   'code'    : '1025',
   'message' : '信息上传不对'
} );


define( '3000', {
   'code'    : '3000',
   'message' : '服务器请求错误'
} );


define( '3001', {
   'code'    : '3001',
   'message' : '请求参数错误!'
} );


define( '3002', {
   'code'    : '3002',
   'message' : '验证码不能为空!'
} );


define( '3003', {
   'code'    : '3003',
   'message' : '尚未登录!'
} );

define( '3004', {
   'code'    : '3004',
   'message' : '类型不能为空!'
} );

define( '3005', {
   'code'    : '3005',
   'message' : '标题不能为空!'
} );

define( '3006', {
   'code'    : '3006',
   'message' : '封面不能为空!'
} );

define( '3007', {
   'code'    : '3007',
   'message' : '类型不能为空!'
} );

define( '3008', {
   'code'    : '3008',
   'message' : '内容不能为空!'
} );

